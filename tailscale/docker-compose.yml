version: "3.8"

services:
  tailscale:
    image: tailscale/tailscale:stable
    container_name: tailscale
    hostname: fajar-wsl        # 🔹 customize freely
    restart: unless-stopped
    network_mode: "host"
    privileged: true
    volumes:
      - ./tailscale-state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    command: tailscaled
