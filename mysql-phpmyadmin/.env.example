# MySQL Configuration
MYSQL_DATABASE=app_db
MYSQL_USER=app_user
MYSQL_PASSWORD=ChangeMe123!
MYSQL_ROOT_PASSWORD=RootPass123!
MYSQL_ROOT_HOST=%
MYSQL_PORT=33060

# phpMyAdmin Configuration
PHPMYADMIN_PORT=33061

# Database Connection Details (for applications)
DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:${MYSQL_PORT}/${MYSQL_DATABASE}
DATABASE_HOST=mysql
DATABASE_PORT=${MYSQL_PORT}
DATABASE_NAME=${MYSQL_DATABASE}
DATABASE_USERNAME=${MYSQL_USER}
DATABASE_PASSWORD=${MYSQL_PASSWORD}

# phpMyAdmin Connection Details
PHPMYADMIN_URL=http://localhost:${PHPMYADMIN_PORT}
PHPMYADMIN_USER=${MYSQL_USER}
PHPMYADMIN_PASSWORD=${MYSQL_PASSWORD}

# Example: docker compose exec mysql mysql -u root -p"${MYSQL_ROOT_PASSWORD}" \
#   -e "GRANT ALL ON my_new_db.* TO '${MYSQL_USER}'@'%'; FLUSH PRIVILEGES;"
